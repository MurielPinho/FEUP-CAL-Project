#include "graphviewer.h"
#include <string>
#include <fstream>
#include <algorithm>
#include <vector>
#include <sstream>

using namespace std;

void displayGraph(string map);
GraphViewer* loadGraph(string map);
